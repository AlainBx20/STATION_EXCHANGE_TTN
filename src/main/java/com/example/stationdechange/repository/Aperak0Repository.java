package com.example.stationdechange.repository;

import com.example.stationdechange.entity.Aperak0;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Aperak0Repository extends JpaRepository<Aperak0, String> {
}
